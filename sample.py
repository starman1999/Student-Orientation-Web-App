# import psycopg2
#
# conn = psycopg2.connect(
#     database="orient",
#     user="root",
#     password="toor",
#     host="localhost",
#     port='5432'
# )
# cur = conn.cursor()
# cur.execute("INSERT INTO etudiants(id, name, matricule)values(1, 'mounir',161)")
# conn.commit()
